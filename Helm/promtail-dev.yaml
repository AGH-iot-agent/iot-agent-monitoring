image:
  registry: docker.io
  repository: grafana/promtail
  tag: 3.4.2
  pullPolicy: IfNotPresent

config:
  clients:
    - url: http://iot-agent-loki:3100/loki/api/v1/push

extraScrapeConfigs: |
  - job_name: kubernetes-pods
    kubernetes_sd_configs:
      - role: pod
